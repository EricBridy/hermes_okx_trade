# hermes_okx_trade

OKX 永续合约自动化交易系统

## 当前版本：v6.0 — 双通道策略

基于 **极端资金费率反向 + 动量顺势评分** 的双通道交易系统，针对 OKX USDT-SWAP 永续合约市场。系统全量扫描 300+ 个合约品种，通过两条独立信号通道筛选交易机会。

## 核心特性

- **双通道开仓**：通道A（极端FR反向）+ 通道B（动量顺势评分），互不干扰
- **多要素评分**：动量、放量、趋势一致性、链上信号、FR方向，5维度综合评分
- **追踪止损**：浮盈1.5%后激活，最高点回撤0.8%自动平仓锁利
- **紧急止盈**：浮盈达6%立即落袋，防暴涨后回撤
- **链上数据整合**：Binance Web3 API（聪明钱信号、热门话题、资金流入），120秒刷新
- **重启安全恢复**：自动加载已有持仓和algo单，清理残留
- **裸仓保护**：每轮验证所有持仓都有TP/SL挂单，无挂单则强制平仓

## 策略逻辑

### 通道A：极端FR反向（复盘验证唯一稳定盈利）

| 条件 | 阈值 | 说明 |
|------|------|------|
| 资金费率 | \|FR\| > 0.05% | 极端费率 = 散户集中方向 |
| 方向 | 只做FR<0做多 | 复盘：FR<0做多赚$7.66，FR>0做空全亏 |
| 放量 | > 均量130% | 确认主力行动 |
| 仓位 | 90% | 高确信度信号，重仓 |

**逻辑**：FR极端负 = 散户集中做空 → 主力拉盘爆空 → 做多跟随

### 通道B：动量顺势评分（不依赖FR，靠短期行情）

| 评分要素 | 分值 | 说明 |
|----------|------|------|
| 5m涨跌幅度 | 1-3分 | >0.5%=1, >1%=2, >2%=3 |
| 1m放量倍数 | 1-3分 | >1.5x=1, >2x=2, >3x=3 |
| 1m趋势一致性 | 1-2分 | 连涨/跌>10根=1, >12根=2 |
| 链上信号 | 0-4分 | 聪明钱买=+2, 热门话题=+2 |
| FR方向一致 | +1分 | FR与趋势同向 |

- **触发**：综合评分 ≥ 5（满分12）
- **方向**：顺势（涨做多，跌做空）
- **仓位**：60%

### 持仓管理

| 机制 | 阈值 | 说明 |
|------|------|------|
| 追踪止损 | 激活1.5%, 距离0.8% | 锁住利润，不坐电梯 |
| 紧急止盈 | 6% | 暴涨时立即落袋 |
| 时间止损 | 15分钟 | 横盘<0.3%则砍仓释放资金 |
| 连亏暂停 | 3次连亏 → 暂停30分钟 | 保护本金 |
| 品种冷却 | 同一品种20分钟内不重复开仓 | 避免过度交易 |
| 最大持仓 | 2个 | 通道A和通道B各最多1个 |

### 仓位动态分配

当同时有2个候选需要开仓时，按权重动态分配余额（总仓位≤95%）：

| 通道 | 权重 | 示例($16余额) |
|------|------|---------------|
| 通道A | 1.5 | ~57% = $9.12 |
| 通道B | 1.0 | ~38% = $6.08 |

单仓模式时保持原始比例（通道A 90%，通道B 60%）。

## 参数配置

```python
# 基础
LEVERAGE = 5
TP_PCT = 0.03          # 止盈 3%
SL_PCT = 0.015         # 止损 1.5%
TRAIL_ACTIVATE = 0.015  # 追踪止损激活 1.5%
TRAIL_DISTANCE = 0.008  # 追踪距离 0.8%
TIME_STOP_SEC = 900     # 时间止损 15分钟
SCAN_INTERVAL = 30      # 扫描间隔 30秒
COOLDOWN_SEC = 1200     # 品种冷却 20分钟
MAX_CONCURRENT = 2      # 最大持仓 2个

# 通道A
FR_EXTREME_THRESHOLD = 0.0005   # |FR| > 0.05%
FR_MAX_THRESHOLD = 0.01         # |FR| > 1% 跳过
CHANNEL_A_VOL_SPIKE = 1.3       # 放量 1.3x
CHANNEL_A_POSITION_PCT = 0.90   # 仓位 90%

# 通道B
MOMENTUM_SCORE_THRESHOLD = 5    # 评分 ≥ 5
CHANNEL_B_POSITION_PCT = 0.60   # 仓位 60%

# 共用
MIN_24H_VOL = 2000000           # 最小24h成交量 $200万
OKX_TAKER_FEE = 0.0005          # 0.05% taker
CHAIN_DATA_TTL = 120            # 链上数据刷新 120秒
CHAIN_API_TIMEOUT = 3           # 链上API超时 3秒
```

## 文件说明

| 文件 | 说明 |
|------|------|
| `v6_yao_hunter.py` | v6.0 主程序 — 双通道策略 |
| `v6_state.json` | 运行状态（连亏计数、冷却记录） |
| `v6_trades.log` | 交易日志 |

## 运行环境

- **Python 3.8+**
- **依赖**：requests（无额外依赖）
- **交易所**：OKX（需 API Key + Secret + Passphrase）
- **服务器**：轻量级，2 核 2G 即可

## 快速开始

### 1. 配置 OKX API

创建 `~/.okx/config.toml`：

```toml
[default]
api_key = "YOUR_API_KEY"
secret_key = "YOUR_SECRET_KEY"
passphrase = "YOUR_PASSPHRASE"
```

### 2. 运行

```bash
python3 v6_yao_hunter.py
```

### 3. 查看日志

```bash
tail -f ~/.hermes/scripts/v6_trades.log
```

### 4. 停止

发送 Ctrl+C 或 `pkill -f v6_yao_hunter`

---

## 更新日志

### v6.0（2026-05-08）

**重大重构 — 双通道策略**

- 新增通道B：动量顺势评分系统（5维度，满分12分，≥5触发）
- 通道A优化：复盘验证后只做FR<0做多，删除FR>0做空（全亏）
- 通道A仓位90%，通道B仓位60%，独立运行
- 综合评分：动量幅度 + 放量倍数 + 趋势一致性 + 链上信号 + FR方向一致
- 1m/5m双时间框架K线分析
- 仓位从1个恢复为2个（两个通道各1个）

**v6.0 Bug修复（05-08 全面审计，13项修复）：**

| Bug | 问题 | 修复 |
|-----|------|------|
| #1 | 连续开仓时余额重复使用，第二仓超扣 | 每开一仓前重新查询余额 |
| #2 | get_balance无异常保护，details空数组IndexError | 添加try/except和长度检查 |
| #3 | total_pnl只计数(+1)不算金额 | 改为记录实际upl金额，新增trade_count |
| #4 | TIME_STOP统一算亏损，微盈也算连亏 | 区分saved_upl>0则重置连亏 |
| #5 | 杠杆配置LEVERAGE=5与文档一致 | 确认5x，保持不变 |
| #6 | K线API 15并发×2=30请求，超OKX限流 | 降低到10并发，扫描范围40→30 |
| #7 | 链上API 8秒timeout白等，chain_bonus永远=0 | timeout降到3秒，刷新间隔60→120秒 |
| #8 | 通道A定义了放量阈值但扫描未检查 | 添加vol_1m>=1.3x过滤条件 |
| #9 | 监控时ticker API失败用入场价兜底→误触发TIME_STOP | API失败返回HOLDING跳过本轮 |
| #10 | emergency_tp = TP_PCT*200表达式误导 | 改为直接写6.0 |
| #11 | import sys未使用 | 删除 |
| #12 | load_state默认值缺少trade_count | 添加trade_count: 0 |
| #13 | okx_post/okx_get中import requests无保护 | 添加try/except ImportError |
| #14 | 小资金下通道A占90%后通道B永远开不了 | 多仓时按权重动态分配余额（A:1.5, B:1.0） |

**v5.1→v6.0 改动依据（全部来自OKX API账单复盘）：**

| 数据结论 | 改动 |
|----------|------|
| JTO FR<0做多赚$7.66 | 通道A只做FR<0做多 |
| AI/PIPPIN FR>0做空全亏 | 删除FR>0做空逻辑 |
| <0.1%FR品种全是噪音 | 通道A阈值降至0.05% |
| 8个品种亏损只有3个盈利 | 通道B引入多要素评分过滤 |
| 手续费$2.47占毛利32% | 冷却延长到20分钟减少交易频率 |

### v5.2（2026-05-08）

- 费率阈值从0.03%提到0.1%
- 成交量门槛从$1M提到$5M
- 放量阈值从1.3x提到1.5x
- 最大持仓从2降到1
- 冷却从10分钟延长到20分钟
- 仓位分层：普通60%，超级极端95%

### v5.1（2026-05-07）

- 链上数据整合（Binance Web3 API）
  - 聪明钱买卖信号
  - AI热门话题关联代币
  - 聪明钱净流入排行
- 追踪止损系统（激活1.5%，距离0.8%）
- 紧急止盈（6%）
- 重启安全恢复（自动加载持仓+algo单）
- 裸仓保护（无挂单则强制平仓）
- 冷却防重复开仓

### v5.0（2026-05-07）

- 从EMA+ADX策略转为极端FR反向策略
- 全市场扫描（304个USDT-SWAP合约）
- 极端费率筛选（|FR|>0.03%）
- 放量确认（>均量110%）
- 反向开仓：FR<0做多，FR>0做空

### v4.2（2026-05-06）

- 多仓并行（最大5个）
- EMA9/21 双时间框架交叉 + ADX 趋势确认
- 并行开仓优化
- 裸仓安全检查 + 3次强制平仓重试

### v4.1（2026-05-06）

- 单仓版本
- K线方向判断 + 资金费率过滤

---

## 风险提示

⚠️ 本项目仅供学习和研究目的。加密货币合约交易风险极高，可能导致本金全部损失。使用前请充分了解风险，作者不对任何交易损失负责。

## License

MIT
