# Hermes v10 部署清单

## 一、需要上传到服务器的文件（共 8 个）

放在本地 Windows 这个目录下：
`e:\Users\xwcEr\Desktop\hermes_okx_trade-7.8\`

| 文件 | 用途 | 必须上传 |
|------|------|---------|
| `hermes_v10_brain.py`     | 学习大脑       | ✅ |
| `hermes_v10_okx.py`       | OKX 客户端     | ✅ |
| `hermes_v10_signals.py`   | 三个信号生成器 | ✅ |
| `hermes_v10_executor.py`  | 下单/平仓执行  | ✅ |
| `hermes_v10_main.py`      | 主程序入口     | ✅ |
| `test_v10_brain.py`       | 单元测试       | ✅（部署脚本会运行）|
| `deploy_v10.sh`           | 自动部署脚本   | ✅ |
| `manage_v10.sh`           | 日常管理脚本   | ✅ |

## 二、不需要上传的文件（保留在本地）

- `v82_*.py` / `adaptive_engine.py` — 旧版本，服务器上已有备份
- `hermes_v9_*.py` — 那是中间实验品
- `test_okx_latency.py` / `probe_ws.py` — 探测脚本，已跑过
- `部署清单.md` / `HERMES_V10_README.md` — 本地参考文档

## 三、上传方式（任选一种）

### 方式 A：用 SCP（推荐，一条命令）

在 Windows PowerShell 里：

```powershell
cd e:\Users\xwcEr\Desktop\hermes_okx_trade-7.8

scp hermes_v10_brain.py hermes_v10_okx.py hermes_v10_signals.py hermes_v10_executor.py hermes_v10_main.py test_v10_brain.py deploy_v10.sh manage_v10.sh root@<服务器IP>:/tmp/hermes_v10/
```

把 `<服务器IP>` 换成实际地址。如果用密钥不用密码，直接传过去；如果要密码，会提示输入。

### 方式 B：用 WinSCP / FileZilla

打开 WinSCP，连服务器，把那 8 个文件拖到 `/tmp/hermes_v10/` 目录。

### 方式 C：服务器上有 git 的话也可以 git pull

不在本次范围内。

## 四、上传后在服务器上执行

```bash
ssh root@<服务器IP>

# 进入上传目录
cd /tmp/hermes_v10

# 给脚本可执行权限
chmod +x deploy_v10.sh manage_v10.sh

# 跑部署脚本（自动执行所有检查 + 测试 + 安装）
./deploy_v10.sh
```

部署脚本做的事：
1. 检查 Python 3.11 路径
2. 检查 OKX 配置文件 (`~/.okx/config.toml`)
3. 检查 v82 是否还在跑（若在跑会提示你确认是否停掉）
4. 创建目录 `~/.hermes/scripts/`（已存在则跳过）
5. 备份旧的 v82 状态文件到 `~/.hermes/backup/v82_<日期>/`
6. 复制 v10 文件到 `~/.hermes/scripts/`
7. 运行 `test_v10_brain.py` 验证大脑学习正确
8. 把 `manage_v10.sh` 也复制过去（方便日后管理）
9. 显示下一步要执行的命令

## 五、部署完成后用管理脚本

部署成功后，所有日常操作都用 `manage_v10.sh`：

```bash
cd ~/.hermes/scripts

./manage_v10.sh start     # 后台启动
./manage_v10.sh stop      # 优雅停止（保存大脑状态）
./manage_v10.sh restart   # 重启
./manage_v10.sh status    # 看运行状态、PID、当前持仓数
./manage_v10.sh logs      # 实时跟踪日志（Ctrl+C 退出）
./manage_v10.sh tail      # 看最近 50 行日志
./manage_v10.sh stats     # 看交易统计（胜率/盈亏/各信号表现）
./manage_v10.sh brain     # 看大脑模型状态（权重/Brier/Kelly）
./manage_v10.sh trades    # 看最近 20 笔交易
```
